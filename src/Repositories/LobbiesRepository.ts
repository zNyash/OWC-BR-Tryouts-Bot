import { prisma } from "../lib/prisma";

export class LobbiesRepository {

    public static async Create(user: string, date_timestamp: number) {

    }

    public static async Get(user: string) {

    }

    public static async Update(user: string, date_timestamp: number) {

    }

    public static async Delete(user: string) {
        
    }
}